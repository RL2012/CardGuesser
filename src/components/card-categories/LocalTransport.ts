// Re-exports from the shared multiplayer transport module.
// Kept here so existing imports in this folder don't break.
export { LocalConnection, type LocalPeerInstance, createLocalPeer } from '../../multiplayer/transport'
